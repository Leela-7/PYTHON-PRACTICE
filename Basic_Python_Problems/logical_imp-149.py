x=10 or 20 and 30
print(x) 

# or gives the first truth value
# AND gives the last truth value
# Truth value means non-zero value
#false value means zero value or None or empty value
# Output: 10

# In this case, 10 is non-zero, so it is considered true.

string="Hello" or "World" and "Python"
print(string)
# Output: Hello
# In this case, "Hello" is non-empty, so it is considered true.
# The expression evaluates to the first truthy value, which is "Hello".

a=10,20,
print(type(a))

# Output: <class 'tuple'>
# we can write a tuple without brackets also that specifying with them as commas also